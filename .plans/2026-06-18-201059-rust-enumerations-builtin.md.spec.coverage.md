# Spec Coverage: Rust enum syntax facts and builtin enumeration verifier

## Goal

- Covered by custom `aqc-crate`, `verify`, `docs`, and `dogfood`.

## End State

- Covered by custom `aqc-crate`, `model`, `lint`, `verify`, and `docs`.

## Boundary Decision

- Covered by custom `aqc-crate`.

## Why This Is Shared But Not Overbuilt

- Covered by custom `aqc-crate`.

## New AQC Crate

- Covered by custom `aqc-crate` and `aqc-tests`.

## AQC Implementation Details

- Covered by custom `aqc-crate` and `aqc-tests`.

## Specular Format Change

- Covered by custom `model`, `lint`, `verify`, `docs`, `fixtures`, and `version`.

## Explicit Non-Goals

- Covered by content forbidden checks and custom `aqc-crate`.

## Key Decisions

- Covered by custom `aqc-crate`, `model`, `lint`, `verify`, and `fixtures`.

## Files To Modify

- Covered by tree required checks and custom `aqc-crate`.

## Work Order

- Covered by custom `dogfood`, `version`, and tree required checks.
