{
  "version": 1,
  "verifiers": {
    "dependencies": ["scripts/v.sh"],
    "enumerations": ["scripts/v.sh"],
    "notacategory": ["scripts/v.sh"],
    "custom": ["scripts/v.sh"]
  },
  "requirements": {
    "tree": {
      "required": ["a", "a", " has-space ", "has*glob", "/abs"],
      "exists": ["a"],
      "forbidden": ["[", "a"]
    },
    "content": [
      {
        "files": ["docs/*.txt"],
        "required": ["x", "x"],
        "exists": ["x"],
        "forbidden": ["x"]
      },
      {
        "files": ["docs/*.txt"],
        "required": ["y"]
      }
    ],
    "dependencies": [
      {
        "manifests": ["Cargo.toml"],
        "required": ["dep"]
      },
      {
        "manifests": ["Cargo.toml"],
        "forbidden": ["other"]
      }
    ],
    "exports": [
      {
        "package": "p",
        "required": ["T"],
        "exists": ["U"]
      }
    ],
    "custom": ["not-object"]
  }
}
