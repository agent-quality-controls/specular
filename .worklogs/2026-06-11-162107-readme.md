Summary:
- Added the first human-facing README for Specular.
- The README opens with a five-line summary that explains what Specular is, why to use it, how to install it, how to run it, and what to tell an agent.
- Ran the README through `slopless README.md` until it passed.

Decisions made:
- Kept the first five lines dense and human-facing, then moved detail into short sections.
- Made `specular --help` the source for field names, verifier calls, lint errors, exit codes, and report JSON instead of copying the full interface into the README.
- Explained the three spec patterns early: predefined categories with built-in verifiers, predefined categories that need a verifier, and custom categories.
- Described verifier commands as any script the OS can run, with Python named as the normal example language.

Key files for context:
- `README.md`
- `src/main.rs`
- `src/lib.rs`
- `.worklogs/2026-06-11-153922-specular-rebrand.md`
- `.worklogs/2026-06-11-160211-resume-session.md`

Next steps:
- Push the current branch when ready. It is ahead of `origin/main` because of the prior resume commit plus this README commit.
