Summary
- Added a root-level `code-sessions` helper that resumes this Codex session from the moved `driftless` repo.
- The helper uses the current `CODEX_THREAD_ID` and sets the repo root as the Codex working directory.

Decisions made
- Used `codex resume` with `--dangerously-bypass-approvals-and-sandbox` to match the existing local resume pattern.
- Kept the helper as a bash script so it can be run directly from the repository root.

Key files for context
- `code-sessions`

Verification
- Confirmed the active session ID from `CODEX_THREAD_ID`.
- Confirmed the repository was clean before adding the helper.

Next steps
- Run `./code-sessions` from the repo root to resume this working session.
