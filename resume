#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec codex resume \
  019eb295-d063-71c3-bf77-5210a05f38f7 \
  -C "${repo_root}" \
  --dangerously-bypass-approvals-and-sandbox
